{% extends 'table.html.twig' %}

{% block title %}Chambre{% endblock %}
{% block tablename %}Chambre{% endblock %}
{% block button %}

    <a href="{{ path('app_chambre_index') }}" class="btn btn-dark btn-sm">back to list</a>
    <a href="{{ path('app_chambre_edit', {'id': chambre.id}) }}" class="btn btn-secondary btn-sm  ml-3">edit</a>
{% endblock %}

{% block table %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chambre.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ chambre.numero }}</td>
            </tr>
            <tr>
                <th>Etage</th>
                <td>{{ chambre.etage }}</td>
            </tr>
            <tr>
                <th>
    {{ include('chambre/_delete_form.html.twig') }}
                </th>
            </tr>
        </tbody>
    </table>



{% endblock %}
