<form method="post" action="{{ path('app_chambre_delete', {'id': chambre.id}) }}" onsubmit="return confirm('Ëtes vous sur de vouloirs supprimer l'item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chambre.id) }}">
    <button class="btn btn-danger btn-sm mr-2"><i class="fas fa-trash-alt"></i></button>
</form>
